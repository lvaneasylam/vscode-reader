{
  "name": "engine-sidecar",
  "private": true,
  "scripts": {
    "build": "esbuild sidecar.ts --bundle --platform=node --format=cjs --outfile=../jetbrains-resources/sidecar.cjs --external:fsevents --log-level=warning"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
